kind: Service
apiVersion: v1
metadata:
  name: a3-auth-frontend-service
spec:
  selector:
    app: a3-auth-frontend
  ports:
    - protocol: TCP
      port: 9095
      targetPort: 80
  type: NodePort
